syntax = "proto3";

package std_msgs;
option go_package="data_relay/proto/std_msgs";

message Header {
    uint32 seq      = 1;
    time   stamp    = 2;
    string frame_id = 3;
}

message time {
    int64 sec  = 1;
    int64 nsec = 2;
}